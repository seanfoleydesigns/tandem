// Tandem agent: boot. The overlay, snapshot, loop and voice arrive in M1 to M3.
// This script is the only thing the host page loads. It knows the page through the DOM alone.

console.info('[tandem] agent loaded');

export {};
